import { ModalWindow, ModalOverlay } from './Modal.styled';

export const Modal = () => {
  return (
    <ModalOverlay>
      <ModalWindow>
        <img src="" alt="" />
      </ModalWindow>
    </ModalOverlay>
  );
};
